name: learnscroll
description: "LearnScroll — remplace le doomscrolling par du learnscrolling."
publish_to: 'none'
version: 0.1.0+1

environment:
  # Dart 3.6 = Flutter 3.27, minimum requis pour Color.withValues().
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
